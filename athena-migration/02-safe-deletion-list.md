# 2. Safe Deletion List — OpenCode → Athena

**Status:** FOR APPROVAL. Nothing has been deleted. Every entry below has had dependency verification (who imports / references / executes / depends on it) performed; **no dependents exist** for any entry unless noted.

Risk scale: **LOW** = safe, isolated removal; **MED** = some co-removal/config edits required; **HIGH** = requires coordinated edits, do not batch blindly.

## Group A — Dead code (no dependents anywhere)

| # | Path | Evidence (dependency check) | Co-removal required | Risk |
|---|---|---|---|---|
| 1 | `packages/effect-sqlite-node/` | Only self-reference in `src/index.ts`. Declared in `core/package.json` deps but **0 imports in `core/src`**. `core` has superset impls in `src/database/sqlite.node.ts` + `sqlite.bun.ts`. Nothing executes it. | Remove `"@opencode-ai/effect-sqlite-node": "workspace:*"` from `packages/core/package.json` deps | LOW |
| 2 | `packages/core/src/effect/dfdf` | Plain-text junk (a saved-file path string); not a module, not imported. | None | LOW |
| 3 | `packages/identity/` | Zero repo references; the only references are `sdks/vscode/images/*` symlinks (Group D). `docs/README.md` describes it wrongly as "Identity management". Not a workspace package (no package.json). | Remove `sdks/` (Group D) simultaneously | LOW |
| 4 | `packages/storybook/` | No package depends on it; root `dev:storybook` + `storybook.yml` are the only callers. | Remove `dev:storybook` script, `storybook.yml`; `packages/storybook/debug-storybook.log` | MED (optional — dev tooling loss; keep if 90 stories are valuable to Athena) |
| 5 | `patches/install-korean-ime-fix.sh` | Not referenced in `patchedDependencies`; not a `.patch` file; nothing executes it. | None | LOW |
| 6 | `packages/storybook/debug-storybook.log` | Stale dev log, 29 KB, committed. | With #4 | LOW |
| 7 | `packages/desktop` `native:build` script | Targets `native/` dir which does not exist. | Fix or drop the script line when editing `desktop/package.json` | LOW |

## Group B — Examples / demo / fixtures (Category E)

| # | Path | Evidence | Risk |
|---|---|---|---|
| 8 | `packages/plugin/src/example.ts` | Not exported from `index.ts`; no references. | LOW |
| 9 | `packages/plugin/src/example-workspace.ts` | Not exported; no references. | LOW |
| 10 | `packages/core/src/plugin/layer-map.example.ts` | `.example` tutorial file; not imported. | LOW |
| 11 | `packages/llm/example/` | Scratchpad `call-sites.md` + `tutorial.ts`; not executed by tests/build. | LOW |
| 12 | `packages/sdk/js/example/example.ts` | Doc example; not part of build. | LOW |
| 13 | `packages/effect-drizzle-sqlite/examples/` | Usage example (`basic.ts`); not referenced. | LOW |
| 14 | `artifacts/` (glm52-rise-video) | Remotion marketing project + ~7.8 MB rendered MP4s; nothing references it. | LOW |
| 15 | `perf/test-suite.md` | Finished benchmark log; not referenced. | LOW |
| 16 | `screenshot-uk.png` | Marketing image at root; not referenced. | LOW |

## Group C — Localized docs & marketing telemetry (Category C/D)

| # | Path | Evidence | Risk |
|---|---|---|---|
| 17 | `README.{ar,bn,br,bs,da,de,es,fr,gr,it,ja,ko,no,pl,ru,th,tr,uk,vi,zh,zht}.md` | 23 locales; `docs-locale-sync.yml` is disabled (`if: false`); no code references. | LOW |
| 18 | `STATS.md` | Generated by `script/stats.ts`, updated by `stats.yml`. | Remove together: `STATS.md` + `script/stats.ts` + `.github/workflows/stats.yml` | LOW |
| 19 | `.DS_Store` (root + all dirs) | macOS metadata junk. | None | LOW |

## Group D — Commercial SaaS + standalone products (Category D)

These are verified leaf packages: none are imported by the core product. All require coordinated removal with their deployment/infra and CI.

| # | Path | Evidence | Co-removal required | Risk |
|---|---|---|---|---|
| 20 | `packages/console/` (app, core, function, mail, resource, support) | Depend only on `@opencode-ai/console-*` siblings + `@opencode-ai/ui`. No product package imports them. | Remove `packages/console/*` from root `workspaces`; `dev:console` script; `infra/console.ts`; console-related SST resources; `sst-env.d.ts` refs | MED |
| 21 | `packages/stats/` (app, core, server) | Depend only on `@opencode-ai/stats-*` + `@opencode-ai/ui`; SST-deployed (AWS). | Remove `packages/stats/*` workspaces glob; `dev:stats` script; `infra/lake.ts` + `infra/stats.ts`; `stats.yml` | MED |
| 22 | `packages/web` | devDepends on `opencode` CLI (one-way); nothing depends on it. | Remove `docs-update.yml`, `docs-locale-sync.yml`; `packages/web` from workspaces | MED |
| 23 | `packages/enterprise` | Uses ui/session-ui/core/sdk one-way; nothing depends on it. Deployed by `infra/enterprise.ts`. | `infra/enterprise.ts`; root workspaces glob (already `packages/*`) | LOW |
| 24 | `packages/function` | Standalone Cloudflare Worker; deployed by `infra/app.ts`; nothing imports it. | `infra/app.ts` share/feishu/token sections | LOW |
| 25 | `packages/slack` | Uses `@opencode-ai/sdk` one-way; nothing depends on it. | None | LOW |
| 26 | `github/` | Standalone composite GitHub Action product; its own publish workflows. | `publish-github-action.yml`, `release-github-action.yml` | LOW |
| 27 | `sdks/` (vscode) | Standalone VS Code extension; `publish-vscode.yml`; symlinks to `packages/identity`. | Remove with `packages/identity` (#3) + `publish-vscode.yml` | LOW |
| 28 | `infra/`, `sst.config.ts`, `sst-env.d.ts` | SST provisioning for #20–#24 + opencode.ai backend (Cloudflare/PlanetScale/AWS/Stripe/Honeycomb). | Remove AFTER #20–#24; remove root `sst` devDependency; regenerate/remove per-package `sst-env.d.ts` (26 files) | MED |
| 29 | `nix/`, `flake.nix`, `flake.lock` (optional) | Nix distribution; `nix-eval.yml` + `nix-hashes.yml`. | Remove Nix CI if removing | MED (optional) |

## Group E — Community / CI workflows (Category D)

| # | Path | Evidence | Risk |
|---|---|---|---|
| 30 | `.github/workflows/beta.yml`, `publish-python-sdk.yml` (commented-out), `compliance-close.yml`, `duplicate-issues.yml`, `triage.yml`, `opencode.yml`, `review.yml`, `close-issues.yml`, `close-prs.yml`, `pr-management.yml`, `pr-standards.yml`, `notify-discord.yml`, `stats.yml`, `deploy.yml`, `docs-update.yml`, `docs-locale-sync.yml`, `publish-vscode.yml`, `publish-github-action.yml`, `release-github-action.yml`, `nix-eval.yml`, `nix-hashes.yml`, `storybook.yml` (if storybook removed) | Team/community/bot automation referencing the removed groups above. | All depend on Group A–D removals | LOW–MED |
| 31 | `.github/CODEOWNERS`, `.github/TEAM_MEMBERS` | Team ownership for a 21-person upstream org; not needed for personal Athena repo. | Replace with Athena ownership | LOW |

## Group F — Conditional keeps (NOT deleted, but flagged)

| Path | Reason flagged | Decision |
|---|---|---|
| `packages/app/vendor/opencode-ai-client-1.17.13-v2.tgz` | Two packages pin to an old prebuilt client; do NOT delete — but **plan to migrate** to published `@opencode-ai/client` so the tarball can be dropped. | KEEP (migrate later) |
| `packages/schema/src/v1/` | Explicitly temporary (per AGENTS.md) but still imported by legacy runtime paths. | KEEP until legacy runtime retired |
| `packages/core/src/github-copilot/` | Self-described temporary; still wired into provider catalog. | KEEP until Athena Model Orchestrator lands |
| `.opencode/` | Repo-local agent config incl. team triage bots. | TRIM (keep `opencode.jsonc`, `skills/`; remove `tool/github-*.ts`, `agent/triage.md`, team rosters) |

---

## Deletion order recommendation (Phase 5, after approval)

1. **Stage 1 — Low-risk dead code + examples:** Groups A + B + C (no build impact; run typecheck after).
2. **Stage 2 — Leaf product removal:** Groups D items #22–#27 (web, enterprise, function, slack, github, sdks). Adjust `workspaces`, scripts, delete co-referenced workflows. Run typecheck + core tests.
3. **Stage 3 — SaaS removal:** #20, #21, #28 (console, stats, infra, sst configs). Run typecheck + build.
4. **Stage 4 — CI normalization:** Group E. Run `bun turbo test` (opencode, core, app, ui, session-ui) + typecheck.
5. **Stage 5 — Docs/rebrand (Athena):** README rewrite, CONTEXT.md, AGENTS.md rename, remove localized READMEs.

**Gate:** After each stage, execute the Build Validation Checklist (deliverable 5). Rollback = `git checkout` (single-commit repo, so snapshot first).
